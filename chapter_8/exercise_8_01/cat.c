#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  /**
   * TODO:
   * - [ ] Check if the there are multiple file names as args 
   *    - [ ] If not, copy the content from stdin to stdout
   *    - [ ] If yes, open and copy each file content to stdout
   * - [ ] Make sure there are error checking at every step
   */

  exit(EXIT_SUCCESS);
}
